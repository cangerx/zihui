# 部署说明

生产环境上线「云控端」「桌面端」「授权管理端」。凭据均为占位符，请按实际环境填写真实值。

---

## 一、云控端 agent-admin 部署

技术栈：PHP 8.0 (Laravel 9) + React 19 + MySQL 8.0。Web 根目录指向 `backend/public`。首次部署可用 `install.php` 向导。

### 1.1 服务器要求

- PHP 8.0.x，扩展：`pdo_mysql`、`mbstring`、`openssl`、`fileinfo`、`gd`（图像处理）、`bcmath`、`curl`、`zip` 等 Laravel 常规扩展。
- MySQL 8.0，库字符集 utf8mb4 / utf8mb4_unicode_ci。
- Web 服务器 Nginx / Apache（宝塔面板亦可）。
- Composer 2.x（服务器上装依赖）。

### 1.2 先构建前端产物（本地或 CI）

前端产物需构建后随后端一起上传（后端 `public/admin`、`public/docs` 目录本包已剔除，需重建）：

```bash
# 管理后台
cd agent-admin/frontend && npm install && npm run build      # → ../backend/public/admin

# 用户文档站
cd agent-admin/docs-frontend && npm install && npm run build  # → ../backend/public/docs
```

### 1.3 部署后端

将 `agent-admin/backend/`（含刚构建出的 `public/admin`、`public/docs`）上传服务器，然后二选一：

**方式 A：可视化安装向导（推荐首次部署）**

```bash
cd backend
composer install --no-dev --optimize-autoloader
# 确保 storage/ bootstrap/cache/ 可写
```

浏览器访问 `https://你的域名/install.php`，按 4 步向导完成：
1. 环境检测（PHP 版本 / 扩展 / 函数）
2. 权限检测（storage、bootstrap/cache、public 可写）
3. 数据库连接测试（默认 127.0.0.1:3306）
4. 站点标题 + 管理员账号密码

完成后自动写入 `.env`、建表、生成 `storage/installed.lock`（再访问 `/install.php` 返回 404 防重装）。

**方式 B：手动命令行**

```bash
cd backend
composer install --no-dev --optimize-autoloader
cp .env.example .env
# 编辑 .env：
#   APP_ENV=production
#   APP_DEBUG=false
#   APP_URL=https://你的域名        # 影响图标/封面等绝对 URL 拼接，务必填真实域名
#   DB_HOST / DB_DATABASE / DB_USERNAME / DB_PASSWORD
php artisan key:generate
php artisan jwt:secret
php artisan migrate --force
```

### 1.4 Web 服务器与权限

- 站点根目录（document root）指向 `backend/public`。
- Nginx 关键规则：Laravel 入口 `try_files $uri $uri/ /index.php?$query_string;`；`/admin`、`/docs` 为 SPA，由 Laravel 路由 fallback 提供入口（框架已处理，无需额外 SPA 配置）。
- **存储目录权限**（重要运维坑）：`storage/` 与 `bootstrap/cache/` 必须归属 Web 运行用户并可写。若出现「部分请求 500 且无日志记录」，常见根因是 `storage/framework/cache/` 下混入了 root 属主的子目录 → 用 Web 用户身份修复：
  ```bash
  chown -R www:www storage bootstrap/cache
  ```
- 如启用配置缓存（`php artisan config:cache`）：改 `.env` 后必须 `php artisan config:clear` 再重新 cache，否则旧值生效。路由缓存（`route:cache`）异常时先 `php artisan route:clear`。

### 1.5 可选组件

- **异步队列**：`.env` 设 `QUEUE_CONNECTION=database`，`php artisan migrate` 建 jobs 表，用 supervisor / NSSM / systemd 常驻 `php artisan queue:work database --queue=kb-index,image,default --tries=2 --timeout=960 --sleep=2`（不变量：worker timeout > 单次 image timeout 900s）。
- **知识库向量库 Qdrant**：见 `agent-admin/docs/知识库向量数据库（Qdrant）部署及配置指南.md`；连接信息在后台「知识库设置」里配，不在 `.env`。
- **在线更新**：`config/version.php` 定义当前版本与更新源（`https://your-cdn-domain.example.com/adminup/version.json`）。已发布的数据库 migration 不要改、不要删，只加新迁移；migration 内只用 `Schema::` / `DB::` 原生 API。

---

## 二、桌面端 agent-desktop 分发

技术栈：Electron 31 + Vue 3 + electron-vite。对外分发的是安装包（Windows NSIS / macOS zip）。

### 2.1 本地出包（自测 / 应急）

```bash
cd agent-desktop
npm install
npm run dist        # Windows：dist/LocalAgent-<版本>-setup.exe
npm run dist:mac    # macOS：x64 + arm64 两个 zip（分架构分别打包）
```

- 需先装好原生模块编译工具链（见「开发说明.md」第一节），否则 better-sqlite3 等编译失败。
- 当前 `electron-builder.yml` **未配置代码签名**（`publish.provider=generic`，仅提供更新地址）。对外分发若需签名/公证，请自行补充证书配置。
- **macOS 未签名分发**：mac 包永无签名公证，首装必被 Gatekeeper 判恶意软件。给用户的安装说明模板见 `agent-admin/docs/macOS未签名安装包-安装说明模板.md`（curl+ditto 零弹窗方案 / xattr -cr 方案）。

### 2.2 自动更新

- `electron-updater` 从 `https://your-cdn-domain.example.com/releases/`（`electron-builder.yml` 的 `publish.url`）拉取更新；mac 只认 zip 作为 auto-update 载体。
- 正式发版通常走「云打包」：由**云控端**直连 GitHub Actions 构建，产物回传到云控存储（跨境较慢时可另外部署本包 `agent-mirror-worker/`）。授权端默认不再对外提供 `/api/build/*`（410），仅保留打包许可开关。本地 `npm run dist` 仅覆盖手工出包。

---

## 三、授权管理端 agent-build 部署（v0.19.1）

技术栈与部署方式与云控端基本一致（PHP 8.0 Laravel 9 + React + MySQL 8.0，Web 根目录指向 `backend/public`）。首次部署可用 `install.php` 向导。

### 3.1 先构建前端产物

```bash
cd agent-build/frontend && npm install && npm run build   # → ../backend/public/admin
```

### 3.2 部署后端（二选一）

**方式 A：可视化安装向导（推荐首次部署）**

上传 `agent-build/backend/`（含构建出的 `public/admin`），`composer install`（勿加 `--no-dev`），浏览器访问 `https://你的域名/install.php` 按向导完成（环境检测 → 权限 → 数据库 → 站点/管理员，GitHub 打包 Token 可留空后补）。完成后写 `storage/installed.lock` 防重装。

**方式 B：手动命令行**

```bash
cd backend
composer install --optimize-autoloader        # 注意：不要加 --no-dev（Laravel 9 依赖 dev 包，缺则整站 500）
cp .env.example .env
# 编辑 .env：APP_ENV=production、APP_DEBUG=false、APP_URL=真实域名、DB 连接、DB_PREFIX=ab_
#   客户端打包默认已下线（BUILD_PACKAGING_RETIRED=true）。GitHub Token 填在云控端，不要填授权端。
#   Skills 审核：按 .env.example 填写 SKILL_REGISTRY_*（密钥仅写服务器，勿提交）
php artisan key:generate
php artisan migrate --force
```

### 3.3 要点

- Web 根目录指向 `backend/public`；`storage/`、`bootstrap/cache/` 属主为 Web 用户且可写（`chown -R www:www storage bootstrap/cache`）。
- 授权管理端自带后台「在线更新」，发版时覆盖代码后执行 `php artisan migrate --force` 并清缓存。
- 收款配置（自助付费 / 开源交付共用微信商户）在后台「收款与交付 → 收款配置」录入；回调地址须本站公网 https 可达。
- 云打包产物跨境回传可部署本包 `agent-mirror-worker/`；对象存储仍见 `agent-build/docs/COS-部署指南.md`。

---

## 四、上线检查清单

- [ ] MySQL 库字符集 utf8mb4 / utf8mb4_unicode_ci
- [ ] `.env`：`APP_ENV=production`、`APP_DEBUG=false`、`APP_URL` 为真实域名、DB/JWT/APP_KEY 已填/生成
- [ ] 已 `php artisan migrate --force`
- [ ] 前端 `public/admin`、`public/docs` 已构建上传
- [ ] `storage/`、`bootstrap/cache/` 属主为 Web 用户且可写
- [ ] Web 根目录指向 `backend/public`，HTTPS 证书就绪
- [ ] 桌面端安装包可正常安装、登录、连通云控端 API
- [ ] 授权管理端 agent-build：前端 `public/admin` 已构建、`migrate --force` 已跑、storage 可写；如启用自助付费/开源交付，已在「收款与交付 → 收款配置」录入微信商户凭据
