# 源码交付说明（2026-08-24）

本文给**已购买本软件源码**的实施方阅读，说明相对上次源码包（2026-08-12）到本快照的产品变化、升级注意，以及本包里有什么、没有什么。

更细的接口与文件级记录仍以各子项目 `CHANGELOG.md` 为准。本地搭建见 `开发说明.md`，生产部署见 `部署说明.md`。

## 1. 本包版本

| 子系统 | 目录 | 上次源码包 | 本快照 |
| --- | --- | --- | --- |
| 云控端 | `agent-admin/` | 1.6.32 | **1.6.43** |
| 桌面端 | `agent-desktop/` | 1.1.1 | **1.3.2** |
| 授权管理端 | `agent-build/` | 0.18.1 | **0.19.2** |
| 云打包回传 worker | `agent-mirror-worker/` | 未收录 | **已收录**（可选组件，跨境拉 GitHub 产物时用） |

本包是**源码快照**，不是现成的服务器安装包或桌面安装包。解压后需按 `开发说明.md` 安装依赖，再按 `部署说明.md` 构建前端并部署。

## 2. 架构关系（相对 8 月 12 日有变化）

- **云控端**：账号、套餐、模型、知识库、内容运营、桌面配置、在线更新；现已具备 **Skills 目录** 与 **客户端云打包执行账本**（生产默认仍可走授权端 remote，需显式切换才在本机闭环）。
- **桌面端**：终端用户客户端。登录云控后使用对话、数字员工、图片/视频创作、技能库等。
- **授权管理端**：云控站点授权、Skills **审核与签名发布**、开源交付收款。默认 **不再对外提供客户端打包入口**（`/api/build/*` 返回 410）；打包调度改由云控直连 GitHub Actions。紧急回切时实现类仍保留。
- **worker**（可选）：部署在到 GitHub 较快的机器上，把构建产物回传到云控存储。未启用时云控仍可自行拉取（跨境会慢）。

三端独立部署、独立发版。Skills 签名密钥、同步口令、下载票据密钥只写在各站点 `.env`，本包示例文件均为空占位。

## 3. 本次更新摘要（2026-08-12 → 2026-08-23）

### 3.1 云控端

**已随 1.6.33–1.6.42 进入正式 CHANGELOG 的能力：**

- 后台栏目按运营任务重组（工作台、用户、模型、计费、客户端、内容运营、文档中心、系统设置）。
- 内容运营工作台：数字员工、灵感、工作流模板改为「本站管理 / 共享市场 / 共享审核」。
- 产品命名对齐桌面：智能体 → 数字员工，创意模板 → 工作流模板。
- 数字员工岗位模板：岗位档案、流程、验收标准、推荐 Skills；桌面投稿与安装有对应契约。
- **Skills 目录**：从授权端增量同步已审核包；分类、推荐、上架；桌面短时下载票据。
- **客户端打包迁入云控**：队列、GitHub 回调、产物抓取与清理可在云控闭环；生产 `CLOUDBUILD_BACKEND=auto` 默认仍 remote。
- 视频开通：导入向导、目录诊断、用户能力工作台、失败原因可读化。
- 官网浅色工作台视觉与下载文案收口。

**已在本源码快照中、尚未单独升云控版本号的改动：**

- 管理后台主色改为蓝（`#2f6fed`），页面底为浅蓝灰。
- 「直充配置」归入「系统设置」。
- 开通模型时可用下拉筛选，并对照官方参考价填写售价。
- 视频供应商增加 MiniMax H3、火山方舟 Seedance。
- 本站共享内容可与授权端 Hub 对齐同步。

从 1.6.32 升级到本快照 **必须执行数据库迁移**（岗位模板字段、Skills 目录表、云构建执行表等）。覆盖代码后在云控后端执行 `php artisan migrate`（生产加 `--force` 须你们自行授权）。

### 3.2 桌面端

**已随 1.1.2–1.3.0 进入正式 CHANGELOG 的能力：**

- 默认对话与当前文件夹工作区：空态直聊、工作区品牌出图/口吻、图库跟文件夹走。
- 爆款复刻成片：口播配音后本机混流；可粘贴公开链用 yt-dlp 入库。
- 技能库双轨：云端已审核包（验签安装）与本地未审核技能同页；数字员工可绑定云端或本地技能。
- 对话、ClawBot、MCP、视频目录等一批稳定性修复（详见桌面 `CHANGELOG.md`）。

**已在本源码快照中、尚未写入桌面 CHANGELOG 的改动：**

- 添加文本/生图渠道时，可点选国内预置（DeepSeek、Kimi、智谱、MiniMax、通义；生图多米/万相），带默认模型 ID。
- 站点只开积分直充时，钱包改为余额英雄区，分「账户余额 / 充值记录 / 消耗明细」。
- 旧数据库升级时补齐 `default_workspace_id` 等列，避免初始化失败。
- 本地视频渠道表单改为密钥 + 模型下拉；设置里增加语音生成渠道。

桌面能力随客户端安装包发布。只更新云控、不重打桌面包时，老客户端会忽略其不认识的字段。

### 3.3 授权管理端

- **Skills 审核**：投稿 ZIP 扫描、ed25519 签名、审核/发布/撤回；向云控提供增量事件与短时下载票据。
- **客户端打包入口默认下线**（0.19.0）。升级必须执行 Skill Registry 相关 migration。
- 0.19.1 修复手工覆盖后「在线更新」类找不到、探活版本号写死等问题。请使用本快照或 0.19.1 更新包，不要再解压残缺的 0.19.0 zip。
- 本快照中：配置类入口归入「系统设置」；后台主色与云控同为蓝。
- **0.19.2**：授权端可托管云控网站更新包（「云控发版」）；公开 `version.json` / zip。云控 **1.6.43** 默认从授权端检查在线更新（`UPDATE_CHECK_URL` 留空即走 `AGENT_BUILD_BASE_URL`）。按客户停更/升级截止日尚未包含在本快照（仍在设计）。

## 4. 升级已有站点时请先看

1. **备份**数据库与 `.env`，不要用本包里的示例文件覆盖生产 `.env`。
2. 云控、授权两端分别 `composer install`、构建前端到 `backend/public/admin`（及云控 `public/docs`），再覆盖 PHP 代码。
3. 两端都要跑 `php artisan migrate`。Skills 与云构建相关表为新增，未迁移时对应页面会报错。
4. 按 `.env.example` **增补**新变量（不要删旧值）：云控侧 `CLOUDBUILD_*`、`SKILL_REGISTRY_*`、`SKILL_CATALOG_TICKET_SECRET`；授权侧 `BUILD_PACKAGING_RETIRED`、`SKILL_REGISTRY_*`。密钥只在服务器填写。
5. 若继续用授权端代发打包：保持云控 `CLOUDBUILD_BACKEND=auto`（生产默认 remote）。若改为云控直连 GitHub：在授权端打开打包许可，在云控填写构建仓，**不要把 GitHub Token 写进授权端**。配置项名称见两端 `.env.example`。
6. 清 Laravel 缓存：`php artisan config:clear && php artisan route:clear && php artisan view:clear`。

## 5. 本包含有与不含

**含有：** 三端源码、`agent-mirror-worker` 源码、`交付说明.md` / `开发说明.md` / `部署说明.md`、契约目录 `contracts/`、示例环境文件（`.env.example` / `deploy.local.example`）。

**不含：**

- `产品转型调研/`（内部调研稿，不随软件交付）
- 内部工作流与研发过程文档（Cursor/Claude 规则、票据、开发计划、方案/分析/事实核查、更新包制作流程、发布规范）
- `.git` 历史、`node_modules` / `vendor`、桌面 `dist` / `out`、前后端已构建的 `public/admin` 与 `public/docs`
- 真实 `.env`、`deploy.local`、代码签名证书目录
- 桌面安装 zip / exe（需自行构建或走云打包）

## 6. 建议阅读顺序

1. 本文
2. `开发说明.md`（本机跑起来）
3. `部署说明.md`（上线）
4. 各子项目 `CHANGELOG.md`（细项变更）
