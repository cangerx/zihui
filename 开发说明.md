# 开发说明

在新设备上搭建「云控端」「桌面端」「授权管理端」的本地开发环境。所有依赖均需重新安装。

---

## 一、基础环境

| 组件 | 版本要求 | 用途 |
| --- | --- | --- |
| Node.js | 建议 20 LTS（桌面端 `@types/node`=20、云控端前端可用 20+） | 前端 / 桌面端 |
| npm | 随 Node 附带 | 包管理 |
| PHP | 8.0.x（`composer.json` platform 锁 8.0.28，Laravel 9） | 云控端/授权管理端后端 |
| Composer | 2.x | PHP 依赖 |
| MySQL | 8.0（字符集 utf8mb4 / utf8mb4_unicode_ci） | 两端数据库 |
| Git | 任意 | 版本管理（本包无 .git，需重新 init） |

**桌面端原生模块编译前置**（`better-sqlite3` / `sqlite-vec` / `@resvg/resvg-js` 需本地编译）：

- Windows：安装 **Visual Studio Build Tools**（含「使用 C++ 的桌面开发」工作负载）+ **Python 3**。`npm install` 时 node-gyp 会调用它们。
- macOS：安装 Xcode Command Line Tools（`xcode-select --install`）。
- 若走国内网络，`.npmrc` 已配置 npmmirror 镜像（registry + electron + electron-builder-binaries），可直接用。

---

## 二、云控端 agent-admin（v1.6.42）

技术栈：PHP 8.0 (Laravel 9) + React 19 (Vite + Ant Design v6) + MySQL 8.0。
含三个可独立启动的部分：`backend`（API）、`frontend`（管理后台 SPA）、`docs-frontend`（用户文档站 SPA）。

### 2.1 后端 backend

```bash
cd agent-admin/backend

# 1. 安装 PHP 依赖
composer install

# 2. 准备环境文件（占位符已脱敏，需按实际填写）
cp .env.example .env          # Windows: copy .env.example .env

# 3. 编辑 .env：至少填好数据库
#    DB_HOST / DB_DATABASE / DB_USERNAME / DB_PASSWORD
#    APP_URL 改成本机访问地址（如 http://localhost:8000）

# 4. 生成应用密钥与 JWT 密钥
php artisan key:generate      # 写入 APP_KEY
php artisan jwt:secret        # 写入 JWT_SECRET（tymon/jwt-auth）

# 5. 建库后执行迁移
php artisan migrate

# 6. 启动开发服务器
php artisan serve             # 默认 http://127.0.0.1:8000
```

要点：
- **数据库字符集**必须 utf8mb4 / utf8mb4_unicode_ci（与 `config/database.php` 一致），否则中文乱码。
- **队列**：`.env` 默认 `QUEUE_CONNECTION=sync`（响应后同步跑图像/知识库任务，零配置）。要真异步 + 失败重试，改 `database` 并常驻 `php artisan queue:work`，详见 `.env.example` 内注释。
- **知识库（Qdrant 向量库）**：连接信息在后台「知识库管理 → 知识库设置」配置，不在 `.env`。部署方式见 `agent-admin/docs/知识库向量数据库（Qdrant）部署及配置指南.md`。
- `storage/`、`bootstrap/cache/` 需可写。若报权限/缓存问题：`php artisan optimize:clear`。

### 2.2 管理后台前端 frontend（React SPA）

```bash
cd agent-admin/frontend
npm install
npm run dev        # http://localhost:3000
```

- 开发服务器默认把 `/api` 代理到**生产域名** `https://your-admin-domain.example.com`（见 `vite.config.ts`）。
- 若要对接**本地后端**，把 `vite.config.ts` 里 `server.proxy['/api'].target` 改成 `http://127.0.0.1:8000`。
- 构建：`npm run build` → 产物输出到 `../backend/public/admin`（`base=/admin/`），由 Laravel 提供入口。

### 2.3 用户文档站 docs-frontend（React SPA）

```bash
cd agent-admin/docs-frontend
npm install
npm run dev        # http://localhost:5174
```

- 同样默认代理 `/api` 到生产域名，可改本地。
- 构建：`npm run build` → `../backend/public/docs`（`base=/docs/`）。

---

## 三、桌面端 agent-desktop（v1.3.0）

技术栈：Electron 31 + Vue 3 + electron-vite + TypeScript；本地用 SQLite（better-sqlite3 + sqlite-vec 做向量检索）。

```bash
cd agent-desktop

# 安装依赖（postinstall 会自动执行 electron-builder install-app-deps，
# 为 Electron ABI 重新编译 better-sqlite3 等原生模块）
npm install

# 启动开发（热重载）
npm run dev

# 仅构建（不打包安装器）
npm run build

# 本地打 Windows 安装包
npm run dist

# 打 macOS 双架构包（x64 + arm64，分架构分别打包避免污染）
npm run dist:mac
```

要点：
- 原生模块（`better-sqlite3` / `sqlite-vec` / `@resvg/resvg-js`）在 `npm install` 阶段本地编译，务必先装好上文的编译工具链，否则报 `node-gyp` / `wrong architecture` 类错误。
- `sqlite-vec` 原生扩展与所有 `.node` 模块通过 `electron-builder.yml` 的 `asarUnpack` 解包到 asar 外，运行时才能按文件路径 `loadExtension`。
- **tsc 只许检查不许发射**：`tsconfig.json` 已配 `noEmit`。手动跑类型检查务必 `tsc --noEmit` 或 `tsc -p <config> --noEmit`——不带 noEmit 的 `tsc -p` 会把 `.js`/`.d.ts` 孪生文件发射到 `src/` 与根目录的 `.ts` 源旁，Vite 优先解析 `.js` 导致「改了 .ts 不生效」（运行旧码）。若不幸出现，删除全部孪生 `.js`/`.d.ts`（保留 `src/renderer/postcss.config.js`、`src/renderer/src/env.d.ts`、`src/main/services/deck/gifenc.d.ts` 这三个合法文件）。
- 桌面端登录并调用云控端 API。开发时若要连本地云控端，需在桌面端网络配置/环境里把服务端地址指向本地后端。
- 打正式安装包对外分发走「云打包」链路（授权管理端 `agent-build` 派发到 GitHub Actions 构建，发布与 push 规范见 `agent-desktop/docs/桌面端发布与push规范.md`）；本地 `npm run dist` 仅用于自测。
- macOS 包无签名公证，首装会被 Gatekeeper 拦截，安装说明模板见 `agent-admin/docs/macOS未签名安装包-安装说明模板.md`。

---

## 四、授权管理端 agent-build（v0.19.1）

技术栈：PHP 8.0 (Laravel 9) + React 19（Vite + Ant Design）+ MySQL 8.0。结构同云控端：`backend`（API + 后台）+ `frontend`（管理后台 SPA）。作用：管理云控端授权、云打包派发、商城授权、开源交付收款等。

### 4.1 后端 backend

```bash
cd agent-build/backend

composer install                 # 安装 PHP 依赖（含 dev 包，勿加 --no-dev）
cp .env.example .env             # Windows: copy .env.example .env
# 编辑 .env：填数据库；APP_URL 改本机地址
php artisan key:generate
php artisan migrate
php artisan serve                # 默认 http://127.0.0.1:8000
```

要点：
- 数据库字符集 utf8mb4 / utf8mb4_unicode_ci；表前缀默认 `ab_`（见 `.env` 的 `DB_PREFIX`）。
- **composer install 不要加 `--no-dev`**：Laravel 9 启动依赖 `spatie/laravel-ignition` / `nunomaduro/collision`（在 require-dev 里），缺失会整站 500。
- **云打包相关配置**（`GITHUB_BUILD_TOKEN` / `GITHUB_BUILD_REPO` / `BUILD_SIGN_SECRET` 等）在 `.env`，`.env.example` 已给模板；不配置不影响后台与授权功能，仅「真实触发 GitHub Actions 打包」需要。
- 首次部署也可用可视化安装向导：浏览器访问 `/install.php`（见部署说明）。

### 4.2 管理后台前端 frontend

```bash
cd agent-build/frontend
npm install
npm run dev                      # 开发服务器（默认把 /api、/admin/api 代理到 http://127.0.0.1:8000）
```

- 构建：`npm run build` → 产物输出到 `../backend/public/admin`（`base=/admin/`），由 Laravel 提供入口。
- 两个免登录公开页：商城授权自助页 `/admin/buy`、开源交付页 `/admin/opensource`（收款走微信 Native，商户凭据在后台「收款与交付 → 收款配置」录入）。

---

## 五、改代码时请注意

- 禁止 AI 风格设计、禁止 emoji 图标；弹窗只加阴影、不加背景遮罩。
- 处理含中文的文件用 PowerShell 7+（pwsh），不要用 PowerShell 5.x（会加 BOM / 损坏中文）。
- 任务完成不自动生成总结类文档；迁移/测试/调试临时脚本执行成功后即时清理。
- **Migration 铁律**：已发布 migration 不改不删不重命名；废弃字段写新 migration；migration 内只用 `Schema::` / `DB::` 原生 API，不 import 业务 Model。
